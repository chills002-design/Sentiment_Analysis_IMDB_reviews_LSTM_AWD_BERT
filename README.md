# Sentiment_Analysis_IMDB_reviews_LSTM_AWD_BERT
Sentiment Analysis of IMDB movie review dataset which is classified as 2 reactions (negative and positive) encoded as 0 and 1. Building a custom LSTM model and comparing it with AWD-ULMFIT methodology and pre-trained BERT model after fine-tuning the model based on IMDB dataset. 
